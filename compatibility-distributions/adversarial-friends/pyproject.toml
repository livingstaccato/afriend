[build-system]
requires = ["setuptools>=77", "wheel>=0.43"]
build-backend = "setuptools.build_meta"

[project]
name = "adversarial-friends"
version = "0.7.0"
description = "Compatibility package for afriend"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
dependencies = ["afriend==0.7.0"]

[tool.setuptools]
packages = []
